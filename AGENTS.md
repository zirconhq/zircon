James is a personal assistant app.

## MOST IMPORTANT RULES
- **ALWAYS** run `turbo ci --output-logs=errors-only` on root directory after every code change to ensure that init, build, check, lint and tests pass for all projects. 
- **ALWAYS** validate changes that could affect the frontend using the browser before finishing the task.
- **ALWAYS** implement the most minimal implementation to complete the task. Do not add any extra features or code that is not required for the task. After completing the task investigate if there are any refactors or optimizations possble and ask if you should implement them. use numbered lists to list the refactors or optimizations you have found.
