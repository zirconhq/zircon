{
  "name": "@appcited/james-typescript",
  "type": "module",
  "scripts": {
    "init": "pnpm install --frozen-lockfile",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "files": [
    "dist"
  ],
  "exports": {
    "./*": "./dist/*"
  },
  "peerDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.0"
  }
}
