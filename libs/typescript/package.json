{
  "name": "@zircon/typescript",
  "type": "module",
  "files": [
    "dist"
  ],
  "imports": {
    "#/*.json": "./*.json"
  },
  "exports": {
    "./*": "./dist/*"
  },
  "scripts": {
    "init": "pnpm install --frozen-lockfile",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "peerDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.0"
  }
}
